{
  "name": "shortcut-trainer",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.7.0",
    "standard": "^8.5.0"
  },
  "dependencies": {
    "material-ui": "^0.16.4",
    "react": "^15.4.0",
    "react-dom": "^15.4.0",
    "react-redux": "^4.4.6",
    "react-router": "^3.0.0",
    "react-tap-event-plugin": "^2.0.1",
    "redux": "^3.6.0",
    "underscore": "^1.8.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "lint": "standard src/**.js",
    "lint:fix": "standard src/**.js --fix",
    "build": "react-scripts build",
    "deploy": "yarn build && cp CNAME build/ && mv build/index.html build/200.html && surge -p ./build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "standard": {
    "globals": [
      "it"
    ]
  }
}
