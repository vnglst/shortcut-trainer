{
  "name": "shortcut-trainer",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.7.0",
    "standard": "^8.5.0"
  },
  "dependencies": {
    "material-ui": "^0.16.4",
    "react": "^15.4.0",
    "react-dom": "^15.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "lint": "standard src/**.js",
    "lint:fix": "standard src/**.js --fix",
    "build": "react-scripts build",
    "deploy": "yarn build && cp CNAME build/ && surge -p ./build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
